
# Security and privacy

The vertical slice has no runtime network or telemetry APIs; no invoice/NIP/credential/token/certificate/key/URL/script/command/free-text fields; strict byte, depth, event, string and evaluation limits; duplicate-property and unknown-field rejection; no archive parsing or recursive pack discovery; safe diagnostics; and create-new output semantics.
