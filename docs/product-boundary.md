
# Product boundary

KSeF ScenarioCheck evaluates a normalized synthetic observation against a modeled scenario. It does not connect to KSeF, submit invoices, drive integrations or certify legal correctness.

A pass means only that the supplied synthetic observation matches the selected modeled assertions for the named source snapshot and review state.
