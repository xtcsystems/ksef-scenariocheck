
# Third-party notices

The vertical slice has no third-party NuGet dependencies. It uses the .NET runtime and Base Class Library under Microsoft's applicable .NET licenses. CI actions are pinned to immutable commits and are not shipped in the tool package.
