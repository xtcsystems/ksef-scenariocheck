# Third-party notices

The runtime projects use only .NET platform libraries.

GitHub Actions workflows use:

- `actions/checkout` — MIT License;
- `actions/setup-dotnet` — MIT License;
- `actions/upload-artifact` and `actions/download-artifact` — MIT License.

Official KSeF repositories and documentation are referenced but are not copied as runtime source dependencies in the probe.
